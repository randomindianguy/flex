import React from 'react'
import ReactDOM from 'react-dom/client'
import FlexTeardown from './flex-teardown'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <FlexTeardown />
  </React.StrictMode>,
)
